{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "scripts": {
      "build": "npm install && npx prisma generate && npm run build"
    }
  }
  